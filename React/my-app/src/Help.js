const Help = ({ name, as }) => {
  //   console.log(props);
  return (
    <>
      <h1>It just a normal compoenent for telling props</h1>
      <p>{name}</p>
      <h2>{as}</h2>
    </>
  );
};

export default Help;
